import SignupPage from "@/components/auth-sections/SignupPage";

export const metadata = {
    title: "Sign Up – PulseTix",
    description: "Create your PulseTix account and start exploring the world's best events.",
};

export default function Page() {
    return <SignupPage />;
}